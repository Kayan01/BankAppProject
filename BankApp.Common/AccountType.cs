using System;
using System.Collections.Generic;
using System.Text;

namespace BankApp.Common
{
    public static class AccountType
    {
        /// <summary>
        /// Enum that holds all available account types
        /// </summary>
        public enum accountType
        {
            Savings = 1000,
            Current = 0
        }
    }
}
